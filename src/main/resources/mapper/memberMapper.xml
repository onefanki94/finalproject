<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ict.finalproject.DAO.MemberDAO">
    <!-- 회원 가입 시 사용되는 쿼리 -->
    <insert id="memberCreate" parameterType="memberVO">
        insert into t_user(userid, userpwd, username, email, zipcode, addr, birth)
        values(#{userid}, #{userpwd}, #{username}, #{email}, #{zipcode}, #{addr}, #{birth})
    </insert>

    <!-- 로그인 시 사용되는 쿼리 (권한 정보 포함) -->
    <select id="memberLogin" parameterType="map" resultType="MemberVO">
        SELECT idx, userid, userpwd, username
        FROM t_user
        WHERE userid = #{userid}
    </select>

    <!-- ID로 사용자 조회 -->
    <select id="findByUserid" parameterType="String" resultType="com.ict.finalproject.vo.MemberVO">
        SELECT * FROM t_user WHERE userid = #{userid}
    </select>

    <select id="adminLogin" parameterType="map" resultType="MemberVO">
        SELECT idx, userid, userpwd, username
        FROM t_admin
        WHERE userid = #{userid}
    </select>

</mapper>