<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ict.finalproject.DAO.MasterDAO">
    <!-- 관리자 페이지 공지사항 등록 -->
    <insert id="createNotice" parameterType="MasterVO">
        insert into t_notice(title, content, adminidx) values(#{title},#{content}, #{adminidx})
    </insert>

    <!-- 굿즈 상품 테이블 전체 목록 조회 -->
    <select id="getStoreList" resultType="MasterVO">
        select idx, category, title, price, stock, relDT from t_product
    </select>
</mapper>